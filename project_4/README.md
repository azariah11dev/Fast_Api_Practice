# FastAPI Posts API

A simple FastAPI project that demonstrates basic CRUD operations with an in‑memory dictionary of posts.  
This project is designed as a learning exercise for building APIs with FastAPI.

---

## 🚀 Features
- **GET /** → Returns a welcome message or a limited number of posts if `limit` is provided.
- **GET /{id}** → Fetches a single post by its ID.
- **POST /** → Creates a new post using a request body defined by `Create_Post` schema.

---

